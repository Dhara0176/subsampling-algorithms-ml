# subsampling-algorithms-ml
This project investigates whether machine learning models can be trained on small subsets of data while maintaining performance and reducing computational cost.  The project compares five sampling algorithms with three classification models on two datasets.
